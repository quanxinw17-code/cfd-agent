# Script Templates

This directory is a documentation copy of the active runtime templates in:

```text
src/cfd_agent/templates/
```

The runtime code reads templates from `src/cfd_agent/templates/`. Keep this folder in sync when publishing or handing the project to another machine.
